void setup() {
  for (int pin = 2; pin < 14, pin++){
    pinmode(pin, OUTPUT);
  }
  pinmode(A0, OUTPUT);
  pinmode(A1, OUTPUT);
  pinmode(A2, OUTPUT);
  
}

void loop() {
  // put your main code here, to run repeatedly:

}
